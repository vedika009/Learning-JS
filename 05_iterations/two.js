// while loops

let index = 0
while (index <= 10) {
    console.log(`Value is ${index}`);
    index+=2
}

// for array
let myArray = ['flash', 'batman', 'superman']

let j = 0
while(j < myArray.length) {
    console.log(`Value is: ${myArray[j]}`);
    j++
}

// do while loop
console.log("_------_")
let score = 11 // still it will run once 
do {
    console.log(`Score is ${score}`);
    score++
} while(score <= 10)

// comment added
// new branch added named dev